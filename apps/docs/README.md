# docs

The pencilui documentation site — Next.js 15 (App Router) + Fumadocs + Sandpack live playgrounds.

Implemented in **Stage 6** of the plan. Hosts the registry endpoints (`/registry/[name].json`) consumed by the `pencilui` CLI.
