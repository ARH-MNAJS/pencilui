# pencilui (CLI)

`pencilui` is the CLI for the pencilui component library — `init` scaffolds a project, `add <component>` copies the requested component shells into your repo so you own the source.

```sh
npx pencilui@latest init
npx pencilui@latest add button input dialog
```

Implemented in Stage 3 of the plan.
