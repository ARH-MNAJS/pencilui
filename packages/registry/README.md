# @pencilui/registry

Static component manifests consumed by the `pencilui` CLI. `registry.json` is the index; per-component manifests live under `components/`. Generated from component source at docs build time so the registry never drifts from what we test.
